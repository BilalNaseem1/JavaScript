// constants are variables that are declared with the const keyword
// constants cannot be re-assigned
// constants are used to define values that should not be changed

const pi = 3.14;
const gravity = 9.81;

console.log(pi);
console.log(gravity);

// trying to re-assign a constant will result in an error
pi = 3.15;

console.log(pi);